NODE_ENV=development
PORT=4000
MONGODB_URI=
JWT_SECRET=
JWT_EXPIRES_IN=12h
FRONTEND_ORIGIN=http://localhost:5173

# Used only by npm run seed:admin, not read by the server itself
SEED_ADMIN_EMAIL=
SEED_ADMIN_PASSWORD=
SEED_ADMIN_NAME=Admin
