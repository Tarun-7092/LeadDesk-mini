# LeadDesk Mini — frontend

React + TypeScript + Vite + Tailwind CSS v4. See the root `README.md` for
the full product overview, architecture, and setup instructions.

Quick start:

```bash
npm install
npm run dev
```
