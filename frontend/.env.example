# Only needed if the frontend and backend are deployed to different domains
# and you switch from the dev proxy to an absolute API URL in src/api/client.ts.
# Leave unset for local dev (Vite proxies /api to the backend automatically).
VITE_API_URL=
